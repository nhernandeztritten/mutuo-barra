/** Inyectada por `vite.config.ts` desde `package.json`. La enseña Ajustes. */
declare const __APP_VERSION__: string;
/** Inyectada por `vite.config.ts` desde `VITE_BASE`. Dónde vive la app. */
declare const __APP_BASE__: string;
